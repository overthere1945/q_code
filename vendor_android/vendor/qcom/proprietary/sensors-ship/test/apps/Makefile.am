ACLOCAL_AMFLAGS = -I m4

dirs = ssc_drva_test         \
          ssc_sensor_info      \
          see_workhorse

SUBDIRS = $(dirs)

pkgconfigdir = $(libdir)/pkgconfig
pkgconfig_DATA = sensors-test-apps.pc

EXTRA_DIST = $(pkgconfig_DATA)
